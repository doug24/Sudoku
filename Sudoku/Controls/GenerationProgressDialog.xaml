<Window x:Class="Sudoku.GenerationProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        MinWidth="360"
        Title="Sudoku"
        Cursor="Arrow"
        Background="{Binding WindowBackground}"
        Foreground="{Binding WindowForeground}">

    <Window.Resources>
        <Style x:Key="btnStyle" TargetType="{x:Type Button}" BasedOn="{x:Null}">
            <Setter Property="Background" Value="{Binding ButtonBackground}" />
            <Setter Property="BorderBrush" Value="{Binding ButtonBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <DockPanel Background="{Binding WindowBackground}" LastChildFill="True">

        <Border DockPanel.Dock="Bottom"
                Background="{Binding ButtonPanelBackground}"
                Padding="20,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="cancelButton"
                        Content="Cancel"
                        Padding="4"
                        MinWidth="80"
                        IsCancel="True"
                        Click="CancelButton_Click"
                        Style="{StaticResource btnStyle}"/>
            </StackPanel>
        </Border>

        <StackPanel Orientation="Horizontal" Margin="20,20,20,12">
            <ProgressBar IsIndeterminate="True"
                         Width="24" Height="24"
                         Margin="0,0,12,0"/>
            <TextBlock x:Name="messageText"
                       VerticalAlignment="Center"
                       Foreground="{Binding WindowForeground}"
                       TextWrapping="Wrap"
                       MaxWidth="400"/>
        </StackPanel>

    </DockPanel>
</Window>
